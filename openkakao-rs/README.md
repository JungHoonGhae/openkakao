# openkakao-rs

Rust rewrite of OpenKakao CLI with feature parity for read-only REST workflows.

## Build

```bash
cargo build --release
```

## Run

```bash
cargo run -- auth
cargo run -- login --save
cargo run -- chats
```
